{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "A Todo-list site that uses local storage to store users content",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "pages": "git subtree push --prefix dist origin gh-pages"
  },
  "author": "Hussein",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^6.10.0",
    "style-loader": "^3.3.4",
    "webpack": "^5.90.1",
    "webpack-cli": "^5.1.4"
  }
}
